"use client";

import type { FleetItem } from "../fleet/[slug]/page";

type props = {
	data: FleetItem;
	helicopterData?: any;
	children?: any;
};

const TemplateThree = ({ data, helicopterData, children }: props) => {
	return <></>;
};

export default TemplateThree;
